<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SinGo — La Boutique Esclusiva</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,600;0,700;0,800;1,400&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        /* ============================================
           RESET & BASE — LUSSO ITALIANO
        ============================================ */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, sans-serif;
            background: #0A0806;
            color: #E8E2D8;
            min-height: 100vh;
            background-image: 
                radial-gradient(ellipse at 30% 20%, rgba(184, 150, 106, 0.04) 0%, transparent 50%),
                radial-gradient(ellipse at 70% 80%, rgba(184, 150, 106, 0.03) 0%, transparent 50%);
        }

        /* ============================================
           SCROLLBAR ORO
        ============================================ */
        ::-webkit-scrollbar { width: 4px; }
        ::-webkit-scrollbar-track { background: transparent; }
        ::-webkit-scrollbar-thumb { 
            background: linear-gradient(180deg, #B8966A, #D4AF37);
            border-radius: 2px;
        }

        /* ============================================
           FILIGRANA DI SFONDO
        ============================================ */
        .watermark {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) scale(1.8);
            font-family: 'Playfair Display', serif;
            font-size: 18rem;
            font-weight: 700;
            color: rgba(184, 150, 106, 0.025);
            letter-spacing: 0.3em;
            pointer-events: none;
            user-select: none;
            z-index: 0;
            white-space: nowrap;
        }

        .watermark::before {
            content: '✦ ✦ ✦';
            display: block;
            font-size: 2rem;
            letter-spacing: 2em;
            text-align: center;
            color: rgba(184, 150, 106, 0.04);
            margin-bottom: 20px;
        }

        .watermark::after {
            content: '✦ ✦ ✦';
            display: block;
            font-size: 2rem;
            letter-spacing: 2em;
            text-align: center;
            color: rgba(184, 150, 106, 0.04);
            margin-top: 20px;
        }

        /* ============================================
           GREGHE DECORATIVE
        ============================================ */
        .greek-border {
            position: relative;
            padding: 30px 0;
        }

        .greek-border::before,
        .greek-border::after {
            content: '▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬';
            display: block;
            font-size: 0.6rem;
            letter-spacing: 1px;
            color: rgba(184, 150, 106, 0.15);
            text-align: center;
            overflow: hidden;
        }

        .greek-border::before {
            margin-bottom: 16px;
        }

        .greek-border::after {
            margin-top: 16px;
        }

        /* ============================================
           NASTRO IN ORO
        ============================================ */
        .gold-ribbon {
            display: inline-block;
            background: linear-gradient(135deg, #B8966A, #D4AF37, #B8966A);
            color: #0A0806;
            padding: 6px 24px 6px 20px;
            font-size: 0.65rem;
            text-transform: uppercase;
            letter-spacing: 0.25em;
            font-weight: 600;
            position: relative;
            font-family: 'Inter', sans-serif;
        }

        .gold-ribbon::after {
            content: '';
            position: absolute;
            right: -16px;
            top: 0;
            width: 0;
            height: 0;
            border-top: 16px solid transparent;
            border-bottom: 16px solid transparent;
            border-left: 16px solid #B8966A;
        }

        /* ============================================
           NAVBAR — VERSIONE LUSSO
        ============================================ */
        .navbar-luxe {
            position: sticky;
            top: 0;
            z-index: 100;
            padding: 18px 48px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: rgba(10, 8, 6, 0.85);
            backdrop-filter: blur(24px);
            -webkit-backdrop-filter: blur(24px);
            border-bottom: 1px solid rgba(184, 150, 106, 0.08);
        }

        .nav-logo-luxe {
            font-family: 'Playfair Display', serif;
            font-size: 1.6rem;
            font-weight: 700;
            color: #E8E2D8;
            text-decoration: none;
            letter-spacing: 0.06em;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .nav-logo-luxe .crown {
            font-size: 1rem;
            color: #D4AF37;
        }

        .nav-logo-luxe .gold-dot {
            display: inline-block;
            width: 6px;
            height: 6px;
            background: #D4AF37;
            border-radius: 50%;
            margin: 0 4px;
        }

        .nav-links-luxe {
            display: flex;
            gap: 32px;
            align-items: center;
        }

        .nav-links-luxe a {
            color: rgba(232, 226, 216, 0.5);
            text-decoration: none;
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.15em;
            font-weight: 500;
            transition: color 0.3s ease;
            position: relative;
        }

        .nav-links-luxe a::after {
            content: '';
            position: absolute;
            bottom: -4px;
            left: 0;
            width: 0;
            height: 1px;
            background: linear-gradient(90deg, #B8966A, #D4AF37);
            transition: width 0.4s ease;
        }

        .nav-links-luxe a:hover {
            color: #E8E2D8;
        }

        .nav-links-luxe a:hover::after {
            width: 100%;
        }

        .nav-links-luxe .gold-btn {
            color: #0A0806;
            background: linear-gradient(135deg, #B8966A, #D4AF37);
            padding: 8px 24px;
            border-radius: 100px;
            font-weight: 600;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            font-size: 0.7rem;
            transition: all 0.3s ease;
        }

        .nav-links-luxe .gold-btn::after {
            display: none;
        }

        .nav-links-luxe .gold-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 24px rgba(184, 150, 106, 0.25);
            color: #0A0806;
        }

        /* ============================================
           HERO SECTION
        ============================================ */
        .hero-luxe {
            position: relative;
            z-index: 1;
            padding: 80px 48px 60px;
            text-align: center;
            max-width: 1200px;
            margin: 0 auto;
        }

        .hero-luxe .gold-ribbon {
            margin-bottom: 32px;
        }

        .hero-luxe h1 {
            font-family: 'Playfair Display', serif;
            font-size: 4.2rem;
            font-weight: 700;
            line-height: 1.08;
            letter-spacing: -0.02em;
            margin-bottom: 20px;
            background: linear-gradient(180deg, #FFFFFF 30%, rgba(255, 255, 255, 0.5));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hero-luxe h1 .gold {
            background: linear-gradient(135deg, #B8966A, #D4AF37, #B8966A);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            background-size: 200% 100%;
            animation: shimmer 4s ease-in-out infinite;
        }

        @keyframes shimmer {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }

        .hero-luxe .subtitle {
            font-size: 1.1rem;
            color: rgba(232, 226, 216, 0.4);
            font-weight: 300;
            letter-spacing: 0.05em;
            max-width: 560px;
            margin: 0 auto 40px;
            line-height: 1.7;
        }

        .hero-luxe .hero-actions {
            display: flex;
            gap: 16px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn-hero-primary {
            padding: 14px 40px;
            background: linear-gradient(135deg, #B8966A, #D4AF37);
            border: none;
            border-radius: 100px;
            color: #0A0806;
            font-weight: 600;
            font-size: 0.85rem;
            cursor: pointer;
            transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
            font-family: 'Inter', sans-serif;
            letter-spacing: 0.04em;
            text-transform: uppercase;
        }

        .btn-hero-primary:hover {
            transform: translateY(-3px) scale(1.02);
            box-shadow: 0 16px 40px -12px rgba(184, 150, 106, 0.3);
        }

        .btn-hero-secondary {
            padding: 14px 32px;
            background: transparent;
            border: 1px solid rgba(184, 150, 106, 0.2);
            border-radius: 100px;
            color: rgba(232, 226, 216, 0.6);
            font-weight: 500;
            font-size: 0.85rem;
            cursor: pointer;
            transition: all 0.3s ease;
            font-family: 'Inter', sans-serif;
            text-transform: uppercase;
            letter-spacing: 0.04em;
        }

        .btn-hero-secondary:hover {
            border-color: rgba(184, 150, 106, 0.4);
            color: #E8E2D8;
            background: rgba(184, 150, 106, 0.04);
        }

        /* ============================================
           SEZIONE PRODOTTI IN EVIDENZA
        ============================================ */
        .featured-section {
            position: relative;
            z-index: 1;
            padding: 40px 48px 60px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .featured-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
            margin-bottom: 32px;
        }

        .featured-header h2 {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            font-weight: 600;
            color: #E8E2D8;
        }

        .featured-header .view-all {
            color: rgba(232, 226, 216, 0.3);
            text-decoration: none;
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        .featured-header .view-all:hover {
            color: #D4AF37;
        }

        .featured-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
        }

        .featured-card {
            background: rgba(255, 255, 255, 0.02);
            border: 1px solid rgba(255, 255, 255, 0.04);
            border-radius: 16px;
            padding: 28px 24px 24px;
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
            cursor: pointer;
        }

        .featured-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(90deg, transparent, rgba(184, 150, 106, 0.3), transparent);
            opacity: 0;
            transition: opacity 0.4s ease;
        }

        .featured-card:hover {
            background: rgba(255, 255, 255, 0.04);
            border-color: rgba(184, 150, 106, 0.1);
            transform: translateY(-4px);
        }

        .featured-card:hover::before {
            opacity: 1;
        }

        .featured-card .icon {
            font-size: 2.4rem;
            margin-bottom: 16px;
            display: block;
        }

        .featured-card h3 {
            font-family: 'Playfair Display', serif;
            font-size: 1.1rem;
            font-weight: 600;
            color: #E8E2D8;
            margin-bottom: 8px;
        }

        .featured-card p {
            font-size: 0.85rem;
            color: rgba(232, 226, 216, 0.35);
            line-height: 1.6;
            font-weight: 300;
        }

        .featured-card .price {
            display: block;
            margin-top: 16px;
            font-weight: 600;
            color: #D4AF37;
            font-size: 1.1rem;
        }

        .featured-card .price .original {
            color: rgba(232, 226, 216, 0.15);
            text-decoration: line-through;
            font-weight: 400;
            font-size: 0.85rem;
            margin-left: 8px;
        }

        /* ============================================
           BADGE "ESCLUSIVO"
        ============================================ */
        .exclusive-badge {
            position: absolute;
            top: 12px;
            right: 12px;
            background: rgba(184, 150, 106, 0.12);
            border: 1px solid rgba(184, 150, 106, 0.15);
            padding: 3px 12px;
            border-radius: 100px;
            font-size: 0.55rem;
            text-transform: uppercase;
            letter-spacing: 0.12em;
            color: #B8966A;
            font-weight: 600;
        }

        /* ============================================
           FOOTER — CON GREGHE
        ============================================ */
        .footer-luxe {
            position: relative;
            z-index: 1;
            padding: 40px 48px 32px;
            max-width: 1200px;
            margin: 0 auto;
            text-align: center;
        }

        .footer-luxe .greek-border {
            padding: 20px 0;
        }

        .footer-luxe .footer-copy {
            font-size: 0.7rem;
            color: rgba(232, 226, 216, 0.12);
            letter-spacing: 0.15em;
            text-transform: uppercase;
            font-weight: 400;
        }

        .footer-luxe .footer-links {
            display: flex;
            justify-content: center;
            gap: 32px;
            margin-bottom: 20px;
        }

        .footer-luxe .footer-links a {
            color: rgba(232, 226, 216, 0.15);
            text-decoration: none;
            font-size: 0.7rem;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            transition: color 0.3s ease;
        }

        .footer-luxe .footer-links a:hover {
            color: rgba(232, 226, 216, 0.4);
        }

        /* ============================================
           RESPONSIVE
        ============================================ */
        @media (max-width: 1024px) {
            .featured-grid {
                grid-template-columns: repeat(2, 1fr);
            }
            .hero-luxe h1 {
                font-size: 3.2rem;
            }
        }

        @media (max-width: 768px) {
            .navbar-luxe {
                padding: 14px 20px;
                flex-wrap: wrap;
                gap: 12px;
            }
            .nav-links-luxe {
                gap: 16px;
                flex-wrap: wrap;
                justify-content: center;
            }
            .hero-luxe {
                padding: 40px 20px 40px;
            }
            .hero-luxe h1 {
                font-size: 2.4rem;
            }
            .featured-section {
                padding: 20px 20px 40px;
            }
            .featured-grid {
                grid-template-columns: 1fr;
            }
            .footer-luxe {
                padding: 20px 20px 24px;
            }
            .watermark {
                font-size: 6rem;
                transform: translate(-50%, -50%) scale(1);
            }
            .featured-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 8px;
            }
        }

        @media (max-width: 480px) {
            .hero-luxe h1 {
                font-size: 1.8rem;
            }
            .nav-links-luxe a {
                font-size: 0.65rem;
            }
            .gold-ribbon {
                font-size: 0.55rem;
                padding: 4px 16px 4px 14px;
            }
            .btn-hero-primary,
            .btn-hero-secondary {
                padding: 12px 24px;
                font-size: 0.7rem;
                width: 100%;
            }
            .hero-luxe .hero-actions {
                flex-direction: column;
                align-items: center;
            }
        }

        /* ============================================
           ANIMAZIONI D'INGRESSO
        ============================================ */
        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .hero-luxe > *,
        .featured-card,
        .featured-header > * {
            animation: fadeUp 0.7s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;
            opacity: 0;
        }

        .hero-luxe > *:nth-child(1) { animation-delay: 0.05s; }
        .hero-luxe > *:nth-child(2) { animation-delay: 0.10s; }
        .hero-luxe > *:nth-child(3) { animation-delay: 0.15s; }
        .hero-luxe > *:nth-child(4) { animation-delay: 0.20s; }
        .featured-card:nth-child(1) { animation-delay: 0.25s; }
        .featured-card:nth-child(2) { animation-delay: 0.30s; }
        .featured-card:nth-child(3) { animation-delay: 0.35s; }
        .featured-header > * { animation-delay: 0.15s; }
    </style>
</head>
<body>

    <!-- FILIGRANA DI SFONDO -->
    <div class="watermark">SIN<span style="color:rgba(184,150,106,0.035);">Go</span></div>

    <!-- ============================================
         NAVBAR LUSSO
    ============================================ -->
    <nav class="navbar-luxe">
        <a href="#" class="nav-logo-luxe">
            <span class="crown">👑</span>
            SIN<span class="gold-dot"></span>GO
        </a>
        <div class="nav-links-luxe">
            <a href="#">Collezione</a>
            <a href="#">Atelier</a>
            <a href="#">Eventi</a>
            <a href="#" class="gold-btn">Accedi</a>
        </div>
    </nav>

    <!-- ============================================
         HERO
    ============================================ -->
    <section class="hero-luxe">
        <div class="gold-ribbon">✦ Edizione Limitata ✦</div>
        <h1>
            L'arte del<br>
            <span class="gold">Supermercato</span>
        </h1>
        <p class="subtitle">
            Dove la tradizione italiana incontra l'eccellenza contemporanea. 
            Ogni prodotto è selezionato con cura per offrirti il meglio.
        </p>
        <div class="hero-actions">
            <button class="btn-hero-primary">Esplora la collezione</button>
            <button class="btn-hero-secondary">Scopri di più</button>
        </div>
    </section>

    <!-- ============================================
         GREGHE DECORATIVE
    ============================================ -->
    <div class="greek-border"></div>

    <!-- ============================================
         PRODOTTI IN EVIDENZA
    ============================================ -->
    <section class="featured-section">
        <div class="featured-header">
            <h2>✦ Selezioni Preziose</h2>
            <a href="#" class="view-all">Vedi tutti →</a>
        </div>

        <div class="featured-grid">
            <!-- Card 1 -->
            <div class="featured-card">
                <span class="exclusive-badge">Esclusivo</span>
                <span class="icon">🍷</span>
                <h3>Vino Barolo</h3>
                <p>Annata 2016, invecchiato 24 mesi in barrique. Selezione delle migliori uve Nebbiolo.</p>
                <span class="price">€ 89,00 <span class="original">€ 120,00</span></span>
            </div>

            <!-- Card 2 -->
            <div class="featured-card">
                <span class="exclusive-badge">Ed. Limitata</span>
                <span class="icon">🧀</span>
                <h3>Parmigiano 48 Mesi</h3>
                <p>Formaggio DOP stagionato 48 mesi, prodotto esclusivamente da latte di montagna.</p>
                <span class="price">€ 34,50 <span class="original">€ 45,00</span></span>
            </div>

            <!-- Card 3 -->
            <div class="featured-card">
                <span class="exclusive-badge">Premium</span>
                <span class="icon">🫒</span>
                <h3>Olio DOP Toscano</h3>
                <p>Olio extra vergine di oliva, raccolto a mano da alberi secolari nel cuore della Toscana.</p>
                <span class="price">€ 28,00 <span class="original">€ 38,00</span></span>
            </div>
        </div>
    </section>

    <!-- ============================================
         GREGHE DECORATIVE
    ============================================ -->
    <div class="greek-border"></div>

    <!-- ============================================
         FOOTER LUSSO
    ============================================ -->
    <footer class="footer-luxe">
        <div class="footer-links">
            <a href="#">Privacy</a>
            <a href="#">Termini</a>
            <a href="#">Contatti</a>
            <a href="#">Newsletter</a>
        </div>
        <div class="footer-copy">
            © 2026 SIN<span style="color:#D4AF37;">GO</span> — La Boutique Esclusiva · Made in Italy
        </div>
    </footer>

    <!-- ============================================
         PICCOLO SCRIPT PER INTERATTIVITÀ
    ============================================ -->
    <script>
        // Effetto di parallasse leggero sulla filigrana
        document.addEventListener('mousemove', function(e) {
            const watermark = document.querySelector('.watermark');
            const x = (e.clientX / window.innerWidth - 0.5) * 8;
            const y = (e.clientY / window.innerHeight - 0.5) * 8;
            watermark.style.transform = `translate(calc(-50% + ${x}px), calc(-50% + ${y}px)) scale(1.8)`;
        });

        // Click sulle card con effetto elegante
        document.querySelectorAll('.featured-card').forEach(card => {
            card.addEventListener('click', function() {
                this.style.transform = 'scale(0.98)';
                setTimeout(() => {
                    this.style.transform = '';
                }, 200);
            });
        });
    </script>

</body>
</html>